type User {
    idx: Int,
    nickName: String,
    allowedUseTerm: Boolean,
    allowedPrivacyTerm: Boolean,
    createdAt: String,
    updatedAt: String
}


type Query {
    getUser(idx: Int!) : User!
}