type Member {
    idx: Int,
    nickName: String,
    allowedUseTerm: Boolean,
    allowedPrivacyTerm: Boolean,
    createdAt: String,
    updatedAt: String
}


type Query {
    getMember(idx: Int!) : Member!
}