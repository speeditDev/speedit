# Environment Variable
DATABASE_HOST: localhost:3307
DATABASE_USER: root
DATABASE_PASSWORD: adfaie83ma
DATABASE_NAME: book-plate

ALADIN_API_URL: http://www.aladin.co.kr/ttb/api
ALADIN_API_TTB_KEY: ttbrolroralra1814001
ALADIN_API_VERSION: 20131101

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DATABASE_HOST}/${DATABASE_NAME}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    password: ${DATABASE_PASSWORD}
    username: ${DATABASE_USER}
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyJpaCompliantImpl
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: embedded
      schema-locations: sql/schema-mysql.sql
      data-locations:   sql/data-mysql.sql

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: trace